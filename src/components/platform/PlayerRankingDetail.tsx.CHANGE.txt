APPLY TO: src/components/platform/PlayerRankingDetail.tsx

1. Add:
   import { TitleRenderer } from './TitleRenderer';

2. In the equipped-title section, replace the text-only title.name badge with:

   <TitleRenderer
     title={title}
     size="sm"
     className="rounded-full border border-amber-300/15 bg-amber-400/[0.07] px-2 py-1"
   />

The existing title object should come from shopProfile.equipped. No title/price logic
belongs in PlayerRankingDetail; the renderer only displays the server-selected artwork.
